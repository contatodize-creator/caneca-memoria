"use client";
import{FormEvent,useEffect,useState}from"react";import{useParams,useRouter}from"next/navigation";import Link from"next/link";import Nav from"../../../components/Nav";import QrCard from"../../../components/QrCard";import{getOwnedMemory,replaceMedia,updateMemory}from"../../../lib/memory-api";import{Memory,MediaType,Theme}from"../../../lib/types";
export default function Editar(){const{slug}=useParams<{slug:string}>();const router=useRouter();const[memory,setMemory]=useState<Memory|null>(null);const[ready,setReady]=useState(false);const[saving,setSaving]=useState(false);const[saved,setSaved]=useState(false);const[error,setError]=useState("");const[newFile,setNewFile]=useState<File|null>(null);const[externalUrl,setExternalUrl]=useState("");useEffect(()=>{getOwnedMemory(slug).then(setMemory).catch(()=>router.replace("/login")).finally(()=>setReady(true))},[slug,router]);if(!ready)return <main><Nav/><div className="formWrap"><div className="empty">Carregando...</div></div></main>;if(!memory)return <main><Nav/><div className="formWrap"><div className="empty">Experiência não encontrada.</div></div></main>;const url=typeof window!=="undefined"?`${window.location.origin}/m/${memory.slug}`:"";function update<K extends keyof Memory>(key:K,value:Memory[K]){setSaved(false);setMemory(prev=>prev?{...prev,[key]:value}:prev)}async function save(e:FormEvent){e.preventDefault();if(!memory)return;setSaving(true);setSaved(false);setError("");try{await updateMemory(slug,{title:memory.title,recipient:memory.recipient,sender:memory.sender,message:memory.message,mediaType:memory.mediaType,theme:memory.theme,active:memory.active,mediaUrl:newFile?undefined:(externalUrl.trim()?externalUrl.trim():undefined)});if(newFile)await replaceMedia(slug,newFile);const fresh=await getOwnedMemory(slug);if(fresh)setMemory(fresh);setNewFile(null);setExternalUrl("");setSaved(true)}catch(err:any){setError(err?.message||"Não foi possível salvar.")}finally{setSaving(false)}}return <main><Nav/><div className="formWrap editExperienceWrap"><div className="formCard"><span className="eyebrow">EDITAR DIZECODE</span><h2>Atualize o conteúdo sem trocar o QR</h2><p className="muted">O código, o endereço e o produto impresso continuam os mesmos. Apenas a experiência aberta pelo QR é atualizada.</p><div className="editPermanentNotice"><strong>QR permanente</strong><span>{memory.slug}</span></div><form className="formGrid" onSubmit={save}><label className="full">Título<input value={memory.title} onChange={e=>update("title",e.target.value)}/></label><label>Destinatário<input value={memory.recipient||""} onChange={e=>update("recipient",e.target.value)}/></label><label>Remetente<input value={memory.sender||""} onChange={e=>update("sender",e.target.value)}/></label><label>Tema<select value={memory.theme} onChange={e=>update("theme",e.target.value as Theme)}><option value="amor">Amor</option><option value="familia">Família</option><option value="pet">Pet</option><option value="aniversario">Aniversário</option><option value="homenagem">Homenagem</option></select></label><label>Tipo de mídia<select value={memory.mediaType} onChange={e=>update("mediaType",e.target.value as MediaType)}><option value="video">Vídeo</option><option value="image">Imagem</option><option value="audio">Áudio</option></select></label><label className="full">Mensagem<textarea value={memory.message} onChange={e=>update("message",e.target.value)}/></label><label className="full">Trocar {memory.mediaType==="video"?"vídeo":memory.mediaType==="image"?"imagem":"áudio"}<div className="fileBox"><input type="file" accept={memory.mediaType==="video"?"video/*":memory.mediaType==="image"?"image/*":"audio/*"} onChange={e=>{setNewFile(e.target.files?.[0]||null);setSaved(false)}}/><div className="small">{newFile?`Novo arquivo: ${newFile.name}`:"Deixe vazio para manter a mídia atual."}</div></div></label><details className="full creatorAdvanced"><summary>Trocar por mídia usando um link</summary><label>URL externa<input value={externalUrl} onChange={e=>{setExternalUrl(e.target.value);setSaved(false)}} placeholder="https://..."/></label></details><label className="toggleRow"><input type="checkbox" checked={memory.active} onChange={e=>update("active",e.target.checked)}/> Experiência ativa</label>{error&&<div className="full errorBox">{error}</div>}{saved&&<div className="full successBox">Alterações salvas. O mesmo QR já abre a versão atualizada.</div>}<div className="full editActions"><button className="button" disabled={saving}>{saving?"Salvando...":"Salvar alterações"}</button><Link className="button secondary" href={`/m/${memory.slug}`} target="_blank">Testar experiência</Link><Link className="textLink" href="/painel">Voltar para Meus QRs</Link></div></form></div><aside className="editQrSide"><span className="eyebrow">ESTE QR NÃO MUDA</span><QrCard url={url} slug={memory.slug}/><p>Você pode alterar mensagem, mídia, tema e destinatário quantas vezes precisar sem reimprimir o código.</p></aside></div></main>}
