"use client";
import Link from "next/link";
import { FormEvent, Suspense, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import Nav from "../../components/Nav";
import { supabase } from "../../lib/supabase";

function LoginForm(){const router=useRouter();const params=useSearchParams();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");const[show,setShow]=useState(false);const requested=params.get("next")||"/painel";const next=requested.startsWith("/")&&!requested.startsWith("//")?requested:"/painel";async function login(e:FormEvent){e.preventDefault();setLoading(true);setError("");setMessage("");const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});setLoading(false);if(error)return setError("Não foi possível entrar. Confira seu e-mail e senha ou recupere o acesso abaixo.");router.push(next);router.refresh()}async function signup(){if(password.length<8)return setError("Para criar a conta, use uma senha com pelo menos 8 caracteres.");setLoading(true);setError("");setMessage("");const{data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{emailRedirectTo:`${window.location.origin}${next}`}});setLoading(false);if(error)return setError("Não foi possível criar a conta. Confira os dados ou tente novamente em alguns instantes.");if(data.session){router.push(next);router.refresh()}else setMessage("Cadastro criado. Enviamos uma confirmação para seu e-mail. Confirme antes de entrar.")}async function recover(){if(!email.trim())return setError("Digite seu e-mail acima para recuperar a senha.");setLoading(true);setError("");setMessage("");const{error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/redefinir-senha`});setLoading(false);if(error)return setError("Não foi possível enviar a recuperação agora. Tente novamente em alguns instantes.");setMessage("Se existir uma conta com esse e-mail, enviaremos as instruções de recuperação.")}return <div className="authWrap"><div className="formCard"><span className="eyebrow">acesso seguro</span><h2 style={{marginTop:14}}>Entre na sua conta</h2><p className="muted">Gerencie seus QRs e experiências em um só lugar.</p><form className="authForm" onSubmit={login}><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email" inputMode="email"/></label><label>Senha<div className="passwordField"><input type={show?"text":"password"} minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Ocultar senha":"Mostrar senha"}>{show?"Ocultar":"Mostrar"}</button></div></label><button className="forgotButton" type="button" onClick={recover} disabled={loading}>Esqueci minha senha</button>{error&&<div className="errorBox" role="alert">{error}</div>}{message&&<div className="successBox" role="status">{message}</div>}<div className="actions"><button className="button" type="submit" disabled={loading}>{loading?"Aguarde...":"Entrar"}</button><button className="button secondary" type="button" onClick={signup} disabled={loading}>Criar conta</button></div><p className="legalInline">Ao criar uma conta, você concorda com os <Link href="/termos">Termos de Uso</Link> e declara ter lido a <Link href="/privacidade">Política de Privacidade</Link>.</p></form><div className="securityNote">🔒 Sua senha é processada pelo sistema de autenticação e não é exibida no painel da Memora.</div></div></div>}

export default function LoginPage(){return <main><Nav/><Suspense fallback={<div className="authWrap"><div className="formCard"><p className="muted">Carregando acesso seguro...</p></div></div>}><LoginForm/></Suspense></main>}
