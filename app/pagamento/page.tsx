import Link from "next/link";import Nav from "../../components/Nav";export default async function Pagamento({searchParams}:{searchParams:Promise<{status?:string}>}){const{status}=await searchParams;const ok=status==="success"||status==="subscription";const pending=status==="pending";return <main><Nav/><div className="authWrap"><div className="formCard" style={{textAlign:"center"}}><span className="eyebrow">pagamento DizeCode</span><h2 style={{marginTop:16}}>{ok?"Pagamento recebido pelo Mercado Pago":pending?"Pagamento em processamento":"Pagamento não concluído"}</h2><p className="muted">{ok?"Estamos confirmando o pagamento com segurança. Seu plano ou crédito será liberado somente após a confirmação do Mercado Pago.":pending?"Assim que o Mercado Pago confirmar, a liberação será feita. Não é necessário criar outro pagamento.":"Nenhuma liberação foi feita. Você pode tentar novamente sem perder seus QRs atuais."}</p><div className="actions" style={{justifyContent:"center"}}><Link href="/painel" className="button">Ir para meu painel</Link><Link href="/planos" className="button secondary">Ver planos</Link></div></div></div></main>}
